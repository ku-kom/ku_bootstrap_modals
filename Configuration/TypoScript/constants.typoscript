ku_bootstrap_modals {
    templates {
        # cat=KU bootstrap modals/file; type=string; label=Layout Root Path: Path to templates (with a trailing slash).
        templateRootPath = EXT:ku_bootstrap_modals/Resources/Private/Templates/
        # cat=KU bootstrap modals/file; type=string; label=Partial Root Path: Path to partials (with a trailing slash).
        partialRootPath = EXT:ku_bootstrap_modals/Resources/Private/Partials/
        # cat=KU bootstrap modals/file; type=string; label=Layout Root Path: Path to layout (with a trailing slash).
        layoutRootPath = EXT:ku_bootstrap_modals/Resources/Private/Layouts/
    }
    assets {
        # cat=KU bootstrap modals/Assets/; type=string; label=CSS assets: Path to stylesheet.
        extraCss =
        # cat=KU bootstrap modals/Assets/; type=string; label=JavaScript assets: Path to javaScript file.
        extraJs = 
    }
}